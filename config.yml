title: 'Metecanpolat01 '
description: 'Heyecan '
template:
  repo: metecanpolat01-template
  name: metecanpolat01
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
